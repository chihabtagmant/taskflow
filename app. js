let projects = [];
function createProject(name,description){
    let project = {
        id: Date.now(),
        name: name,
        description: description,
        members: []
    };
    projects.push(project);
    console.log("project cree : ",project);
}
function addMember(projectId,memberName){
    let project = projects.find(p => p.id === projectId);
    if (project){
        project.members.push({
            id: Date.now(),
            name: memberName
        });
        console.log("membre ajoute");
    }else{
        consoe.log("project introuvable");
    }
    }
    function getAllprojects(){
        return projects;
    }
    function deleteProject(projectId){
        projects = projects.filter(p => p.id !== projectId);
        console.log("project supprime");
    }
    function deleteMember(projectId,memberId){
        let project = projects.find(p => p.id === projectId);
        if (project) {
            project.members =
            project.members.filter(m => m.id !== memberId);
            console.log("membre supprime");
    }
}

    
